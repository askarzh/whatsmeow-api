{
  "mcpServers": {
    "whatsmeow-api": {
      "type": "http",
      "url": "http://localhost:8080/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${WMAPI_AUTH_TOKEN}"
      }
    }
  }
}
